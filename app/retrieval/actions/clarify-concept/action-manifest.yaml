id: clarify-concept
label: Begriff klären
description: Definition eines einzelnen Begriffs.

requires_retrieval: true
requires_prompt: false

position-in-chat: [start, end]

queries:
  - name: lemma-lookup
    chunk_types: [begriff]
  - name: fallback-books
    chunk_types: [book, explanation]
    k: 8
    method: dense

follow_ups:
  - action_id: give-feedback
